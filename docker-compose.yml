services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    volumes:
      - ./backend/chroma_db:/app/chroma_db
      - ~/.azure:/root/.azure:ro
    env_file:
      - ./backend/.env
    networks:
      - careermate-network